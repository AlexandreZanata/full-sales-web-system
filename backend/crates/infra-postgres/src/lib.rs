//! PostgreSQL repository adapters (sqlx) — implemented in later phases.

/// Placeholder until repository adapters land in Phase 2+.
pub fn crate_version() -> &'static str {
    env!("CARGO_PKG_VERSION")
}
